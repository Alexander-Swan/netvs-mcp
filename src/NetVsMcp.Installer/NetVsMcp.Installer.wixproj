<Project Sdk="WixToolset.Sdk/6.0.2">

  <PropertyGroup>
    <OutputName>NetVsMcp.Broker</OutputName>
    <OutputType>Package</OutputType>
    <ProductVersion Condition="'$(ProductVersion)' == ''">$(VersionPrefix)</ProductVersion>
    <BrokerPublishDir Condition="'$(BrokerPublishDir)' == ''">$(MSBuildThisFileDirectory)..\..\artifacts\publish\NetVsMcp.Broker\</BrokerPublishDir>
    <DefineConstants>ProductVersion=$(ProductVersion);BrokerPublishDir=$(BrokerPublishDir)</DefineConstants>
    <!-- ICE38/ICE91/ICE64 flag legacy per-user-profile MSI conventions (HKCU-keypath self-repair,
         roaming-profile RemoveFile tracking) that don't apply to this non-roaming, non-advertised
         tray app, so they're suppressed rather than worked around. -->
    <SuppressIces>ICE38;ICE91;ICE64</SuppressIces>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\NetVsMcp.Broker\NetVsMcp.Broker.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="WixToolset.UI.wixext" Version="6.0.1" />
    <PackageReference Include="WixToolset.Util.wixext" Version="6.0.1" />
  </ItemGroup>

  <Target Name="PublishBrokerForInstaller" BeforeTargets="CoreCompile;Compile;Build">
    <MSBuild
      Projects="..\NetVsMcp.Broker\NetVsMcp.Broker.csproj"
      Targets="Publish"
      Properties="Configuration=$(Configuration);PublishDir=$(BrokerPublishDir);SelfContained=false" />
  </Target>

</Project>

bin/
obj/
.vs/
artifacts/
TestResults/
*.user
*.suo
*.pdb
*.nupkg
# Local staging dir the release workflow creates for MSI/VSIX build artifacts before uploading
# them to a GitHub Release; never meant to be committed (see REL-2 in docs/IMPROVEMENT_PLAN.md).
release-assets/

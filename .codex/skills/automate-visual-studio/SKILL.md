---
name: automate-visual-studio
description: Automate a debuggee's UI, drive a connected browser, or interact with a debuggee's console through the local NetVsMcp MCP tools. Use when Codex needs to screenshot, inspect, or click/type into a debuggee's windows via UI Automation (ui_capture_window, ui_snapshot, ui_get_tree, ui_find_elements, ui_click, ui_set_value, ui_send_keys, ui_wait_for_element, etc.); connect to, navigate, screenshot, query the DOM of, execute JavaScript in, or click elements in a browser via Chrome DevTools Protocol (web_connect, web_navigate, web_screenshot, web_dom_get, web_dom_query, web_js_execute, web_console, web_network, web_element_click, web_element_set_value); or read/write a debuggee console app's stdin/stdout (console_read, console_send, console_get_info) using the repo-neutral .agents automation guide.
---

# Automate Visual Studio

This is a Codex adapter for the repository's agent-neutral Visual Studio UI automation, browser/web debugging, and debuggee console guide.

Read and follow [../../../.agents/skills/automate-visual-studio.md](../../../.agents/skills/automate-visual-studio.md). Treat that file as the canonical workflow and source of truth.
